.row
  .col-sm-12.col-lg-9
    .card
      = form_for [:admin, @rubric, @track], html: {class: 'form-horizontal'} do |f|
        .card-header
          strong
            = @track.persisted? ? @track.name.presence || @track.id : t('.title', rubric_name: @rubric.name)

        .card-body.card-block
          = f.hidden_field :rubric_id
          = render partial: 'admin/partials/errors', locals: {model: @track}

          .row.form-group
            .col.col-md-3
              = f.label :name
            .col-12.col-md-9
              = f.text_field :name, class: 'form-control', autofocus: true

          .row.form-group
            .col.col-md-3
              = f.label :content
            .col-12.col-md-9
              = f.file_field :content

        .card-footer.action-buttons
          = button_tag class: 'btn btn-primary btn-sm' do
            .fa.fa-dot-circle-o
            = t('.apply')

          = link_to admin_rubric_tracks_path(@rubric), class: 'btn btn-danger btn-sm' do
            .fa.fa-ban
            = t('.cancel')
